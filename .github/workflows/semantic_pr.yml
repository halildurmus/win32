name: Semantic PR

on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize
      - reopened

jobs:
  build:
    uses: halildurmus/workflows/.github/workflows/semantic_pr.yml@b71996adb8995d4af04fe79ebf5173ae51ebcfee
