name: task_manager
description: Task Manager
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  ffi: ^2.1.3
  flutter:
    sdk: flutter
  win32:
    path: ../../packages/win32

dev_dependencies:
  lints:
    git:
      url: https://github.com/halildurmus/workflows.git
      path: packages/lints

flutter:
  uses-material-design: true
