name: win32_workspace
publish_to: none

environment:
  sdk: ^3.4.0

dev_dependencies:
  args: ^2.5.0
  ffi:
    git:
      url: https://github.com/halildurmus/native.git
      ref: use-natives-2
      path: pkgs/ffi
  lints: ^4.0.0
  win32:
    path: packages/win32
