name: win32_workspace
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  args: ^2.6.0
  ffi: ^2.1.3
  hooks:
    git:
      url: https://github.com/halildurmus/workflows.git
      path: packages/hooks
  lints: ^5.0.0
  win32:
    path: packages/win32
