/// The GitHub release tag used for downloading prebuilt assets.
///
/// After updating this value, run `dart run tool/generate_asset_hashes.dart`
/// to regenerate the asset hashes in `hashes.g.dart`.
const version = 'win32-prebuilt-assets-v6.1.0';
