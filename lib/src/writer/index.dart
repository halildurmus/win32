/// Represents an index into the `#Blob` heap.
final class BlobIndex {
  const BlobIndex(this.index);

  /// The index into the heap.
  final int index;
}

/// Represents an index into the `#GUID` heap.
final class GuidIndex {
  const GuidIndex(this.index);

  /// The index into the heap.
  final int index;
}

/// Represents an index into the `#Strings` heap.
final class StringIndex {
  const StringIndex(this.index);

  /// The index into the heap.
  final int index;
}

/// Represents an index into the `#US` heap.
final class UserStringIndex {
  const UserStringIndex(this.index);

  /// The index into the heap.
  final int index;
}

/// Represents a row index into the `AssemblyRef` table.
final class AssemblyRefIndex {
  const AssemblyRefIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `ClassLayout` table.
final class ClassLayoutIndex {
  const ClassLayoutIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `Constant` table.
final class ConstantIndex {
  const ConstantIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `CustomAttribute` table.
final class CustomAttributeIndex {
  const CustomAttributeIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `Field` table.
final class FieldIndex {
  const FieldIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `GenericParam` table.
final class GenericParamIndex {
  const GenericParamIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `ImplMap` table.
final class ImplMapIndex {
  const ImplMapIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `InterfaceImpl` table.
final class InterfaceImplIndex {
  const InterfaceImplIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `MemberRef` table.
final class MemberRefIndex {
  const MemberRefIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `MethodDef` table.
final class MethodDefIndex {
  const MethodDefIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `Module` table.
final class ModuleIndex {
  const ModuleIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `ModuleRef` table.
final class ModuleRefIndex {
  const ModuleRefIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `NestedClass` table.
final class NestedClassIndex {
  const NestedClassIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `Param` table.
final class ParamIndex {
  const ParamIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `TypeDef` table.
final class TypeDefIndex {
  const TypeDefIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `TypeRef` table.
final class TypeRefIndex {
  const TypeRefIndex(this.index);

  /// The index into the table.
  final int index;
}

/// Represents a row index into the `TypeSpec` table.
final class TypeSpecIndex {
  const TypeSpecIndex(this.index);

  /// The index into the table.
  final int index;
}
